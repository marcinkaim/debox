#
# Minimal Base Image configuration (for CLI apps)
#
version: 1
image_name: "debox-base-minimal"

image:
  base: "debian:latest"
  
  debian_components: [] 

  packages:
    - "python3"
    - "locales"
    - "sudo"
    - "less"
